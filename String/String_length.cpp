// We can use length(), size() member function to calculate length of the string but
// her i want make code for it.
#include<bits/stdc++.h>
using namespcae std
  int main(){
const char *s ="welcome";
int i;
for(i=0; s[i]!='\0'; i++){
}
return i;
  }

